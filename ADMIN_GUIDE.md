# 🎛️ 管理员后台使用指南

## 📋 概述

摔倒检测系统 v1.4 新增了完整的管理员后台，提供用户管理、系统监控和日志查看等功能。

---

## 🔑 访问方式

### 1. 登录系统

访问主页面并使用管理员账户登录：

```
默认管理员账户:
用户名：admin
密码：admin123
```

⚠️ **首次登录后请立即修改密码！**

### 2. 进入管理后台

登录后，在侧边栏可以看到**管理后台**区域，有两个入口：

- 📊 **仪表板** - 系统概览和实时监控
- 👥 **用户管理** - 用户列表和操作

或者直接访问：
- 仪表板：`http://your-server:8501/system_dashboard`
- 用户管理：`http://your-server:8501/user_management`

---

## 📊 系统仪表板功能

### 核心指标卡片

| 指标 | 说明 |
|------|------|
| **总用户数** | 显示注册用户总数和活跃用户数 |
| **今日登录** | 今天的登录次数和失败次数 |
| **系统运行时间** | 自上次重启以来的运行时长 |
| **磁盘使用率** | 当前磁盘使用情况（超过 80% 会警告） |

### 系统资源监控

实时显示：
- **CPU 使用率** - 进度条 + 百分比
- **内存使用率** - 已用/总量 + 百分比

⚠️ 当使用率超过 80% 时会显示警告，超过 90% 显示错误。

### 用户分析

三个维度的分析：

1. **角色分布**
   - 👑 管理员数量
   - 🔧 操作员数量
   - 👁️ 观察员数量

2. **最近创建的用户**
   - 显示最近 5 个注册的用户
   - 包含创建日期

3. **最近登录的用户**
   - 显示最近 5 个登录的用户
   - 包含最后登录时间

### 登录活动

显示最近 10 条登录记录：
- 登录时间
- 用户名
- IP 地址
- 成功/失败状态

### 快速操作

- 👥 **用户管理** - 跳转到用户管理页面
- 📊 **查看日志** - 查看详细日志
- 🔄 **刷新数据** - 重新加载数据
- ⚙️ **系统设置** - 系统配置（开发中）

---

## 👥 用户管理功能

### 用户列表视图

#### 统计卡片

- **总用户数** - 系统所有用户
- **活跃用户** - 已启用的用户
- **管理员** - 管理员角色数量
- **观察员** - 观察员角色数量

#### 用户表格

显示所有用户的详细信息：

| 列名 | 说明 |
|------|------|
| ID | 用户唯一标识 |
| 用户名 | 登录用户名 |
| 邮箱 | 注册邮箱 |
| 角色 | 用户角色（带图标） |
| 状态 | 🟢 激活 / 🔴 禁用 |
| 创建时间 | 账户创建时间 |
| 最后登录 | 最后一次登录时间 |

### 批量操作

可以执行以下操作：

1. **选择用户 ID** - 输入要操作用户的 ID
2. **选择操作类型**:
   - 🟢 启用账户
   - 🔴 禁用账户
   - 🗑️ 删除用户
   - 📝 重置密码
3. **执行操作** - 点击按钮执行

### 快速操作

针对单个用户的快捷操作：

1. 在下拉菜单中选择用户名
2. 点击对应操作按钮：
   - 🟢 **启用** - 激活账户（仅对已禁用的显示）
   - 🔴 **禁用** - 禁用账户（仅对已启用的显示）
   - 🗑️ **删除** - 删除账户（不能删除自己）
   - 📝 **编辑** - 编辑用户信息（开发中）

### 创建新用户

填写表单创建新用户：

**必填字段**:
- 用户名（3-50 字符）
- 邮箱（有效格式）
- 密码（至少 6 字符）
- 确认密码（必须一致）

**可选字段**:
- 用户角色（观察者/操作员/管理员）
- 创建后立即启用（默认勾选）

**验证规则**:
- ✅ 用户名必须唯一
- ✅ 邮箱必须唯一且格式正确
- ✅ 密码长度至少 6 位
- ✅ 两次密码输入一致

---

## 📊 登录日志功能

### 过滤选项

支持三种过滤方式：

1. **筛选用户** - 查看特定用户的日志
2. **筛选状态** - 只看成功/失败的登录
3. **显示数量** - 10-1000 条记录

### 统计数据

- **总登录次数** - 符合条件的总记录数
- **成功** - 成功登录次数和成功率
- **失败** - 失败登录次数

### 日志详情

每条日志包含：
- 时间 - 登录发生的时间
- 用户 - 登录用户名
- IP 地址 - 来源 IP
- 设备 - User-Agent 信息
- 状态 - 成功/失败及原因

---

## 🔐 安全最佳实践

### 1. 定期审查登录日志

检查异常登录模式：
- 同一 IP 多次失败尝试
- 非工作时间的登录
- 陌生 IP 地址的登录

### 2. 及时禁用离职用户

当员工离职或角色变更时：
1. 立即禁用账户
2. 查看最近的登录活动
3. 必要时删除账户

### 3. 最小权限原则

分配角色时遵循：
- 👁️ **观察员** - 只读权限，适合普通用户
- 🔧 **操作员** - 操作权限，适合运维人员
- 👑 **管理员** - 全部权限，仅限信任人员

### 4. 强密码策略

建议要求：
- 最少 8 个字符
- 包含大小写字母
- 包含数字和特殊字符
- 定期更换密码

---

## 🛠️ 常见问题

### Q1: 如何重置忘记密码的用户？

**方法 1**: 管理员直接修改数据库
```bash
cd /root/.openclaw/workspace/projects/fall-detection-system
python3 -c "
from src.auth.auth_service_sqlite import AuthService
auth = AuthService()
auth.change_password(user_id=1, old_password='xxx', new_password='newpass123')
"
```

**方法 2**: 删除并重新创建用户（适用于普通用户）

### Q2: 如何查看某个用户的所有登录历史？

1. 进入"登录日志"页面
2. 在"筛选用户"下拉框选择该用户
3. 设置合适的显示数量
4. 点击应用

### Q3: 误删了用户怎么办？

目前不支持恢复已删除的用户。需要重新创建账户。

**建议**: 优先使用"禁用"而非"删除"。

### Q4: 如何批量导入用户？

可以使用脚本批量创建：

```python
from src.auth.auth_service_sqlite import AuthService
from src.auth.models import RegisterRequest, UserRole

auth = AuthService()

users = [
    {"username": "user1", "email": "user1@example.com"},
    {"username": "user2", "email": "user2@example.com"},
]

for user_info in users:
    request = RegisterRequest(
        username=user_info["username"],
        email=user_info["email"],
        password="default123",  # 建议首次登录强制修改
        role=UserRole.VIEWER
    )
    auth.register(request)
```

### Q5: 如何限制登录失败次数？

这个功能正在开发中。临时方案是定期检查登录日志，手动禁用可疑账户。

---

## 📈 性能优化建议

### 1. 日志清理

定期清理旧日志以提高查询性能：

```python
from src.database.db import get_db_connection

with get_db_connection() as conn:
    cursor = conn.cursor()
    
    # 删除 90 天前的日志
    cursor.execute("""
        DELETE FROM login_logs 
        WHERE logged_at < datetime('now', '-90 days')
    """)
    
    conn.commit()
```

### 2. 索引优化

数据库已自动创建索引，无需手动优化。

### 3. 缓存策略

对于大型系统，可以考虑：
- 使用 Redis 缓存用户信息
- 缓存常用查询结果
- 定期清理过期会话

---

## 🚀 未来功能规划

### 即将推出（v1.5）
- [ ] 用户编辑功能
- [ ] 批量导入/导出
- [ ] 密码重置邮件
- [ ] 双因素认证

### 计划中（v2.0）
- [ ] 自定义角色和权限
- [ ] 操作审计日志
- [ ] API 密钥管理
- [ ] Webhook 集成

---

## 📞 技术支持

如有问题或建议，请提交 Issue：

- GitHub: https://github.com/nanfeng2021/fall-detection-system
- 文档：AUTH_SQLITE_UPGRADE.md

---

**版本**: v1.4  
**更新日期**: 2026-04-13  
**作者**: 旺财 🐕
